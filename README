About The Project
    At this state this project is a precursor to build a simple production shell.
    The program currently tokenizes a series of strings entered in the command line after initializing the program.

Steps to run:
    1) In the project directory run the command make
    2) In the command line enter ./main to start the shell
    3) To use debug mode enter ./main -Debug in the command line